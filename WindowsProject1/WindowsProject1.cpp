#include <iostream>
#include <cmath>
#include <iomanip>

using namespace std;

const double PI = 3.141592;

int main()
{
	double y;
	double a, b;
	cin >> a >> b;
	y = (sqrt(pow(b, 2) - pow(a, 2)));
	cout << y;
	return 0;
}
 

